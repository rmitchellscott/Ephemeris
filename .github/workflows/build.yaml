name: build-docker

env:
  RMAPI_VERSION: 0.0.30

on:
  push:
    branches:
      - '**'
    tags:
      - 'v*.*.*'
      - 'v*.*'

jobs:
  build:
    name: Build Plain Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/rmitchellscott/ephemeris
          tags: |
            # branch-based tags (e.g. 'main')
            type=ref,event=branch

            # mirror git tag (e.g. 'v1.2.3')
            type=ref,event=tag

            # semver without the 'v' (e.g. '1.2.3')
            type=semver,pattern={{version}}

            # major.minor (e.g. '1.2')
            type=semver,pattern={{major}}.{{minor}}

            # major only (e.g. '1')
            type=semver,pattern={{major}}

            # latest on tag
            type=raw,priority=1000,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/') }}

            # commit short SHA
            type=sha

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Plain Image
        id: docker_build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: ephemeris
          pull: true
          push: ${{ (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')) && !contains(github.ref, 'rmapi') }}
          platforms: linux/amd64,linux/arm64/v8
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-rmapi:
    name: Build RMAPI Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/rmitchellscott/ephemeris
          tags: |
            # For regular tags (v1.2.3), add rmapi suffix
            type=ref,event=tag,suffix=-rmapi${{ env.RMAPI_VERSION }},enable=${{ !contains(github.ref, 'rmapi') }}
            type=semver,pattern={{version}},suffix=-rmapi${{ env.RMAPI_VERSION }},enable=${{ !contains(github.ref, 'rmapi') }}
            type=semver,pattern={{major}}.{{minor}},suffix=-rmapi${{ env.RMAPI_VERSION }},enable=${{ !contains(github.ref, 'rmapi') }}
            type=semver,pattern={{major}},suffix=-rmapi${{ env.RMAPI_VERSION }},enable=${{ !contains(github.ref, 'rmapi') }}

            # For rmapi tags (v1.2.3-rmapi0.0.31), use as-is
            type=ref,event=tag,enable=${{ contains(github.ref, 'rmapi') }}
            type=semver,pattern={{version}},enable=${{ contains(github.ref, 'rmapi') }}
            type=semver,pattern={{major}}.{{minor}},enable=${{ contains(github.ref, 'rmapi') }}
            type=semver,pattern={{major}},enable=${{ contains(github.ref, 'rmapi') }}

            # Branch-based tags (main only)
            type=ref,event=branch,suffix=-rmapi${{ env.RMAPI_VERSION }}

            # Commit short SHA with rmapi suffix
            type=sha,suffix=-rmapi${{ env.RMAPI_VERSION }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push RMAPI Image
        id: docker_build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: ephemeris-rmapi
          build-args: |
            RMAPI_VERSION=${{ env.RMAPI_VERSION }}
          pull: true
          push: ${{ github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/') }}
          platforms: linux/amd64,linux/arm64/v8
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
